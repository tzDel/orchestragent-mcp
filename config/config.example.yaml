# orchestragent-mcp Server Configuration

# Repository settings
repoRoot: "/path/to/your/repository"
baseBranch: "main"

# Test command to run in agent worktrees
testCommand: "go test ./..."

# Worktree settings
worktreeDir: ".worktrees"
